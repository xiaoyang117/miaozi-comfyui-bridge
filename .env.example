# ==========================================================
# 喵梓二号 - 环境变量说明（可选）
#
# 本项目的运行配置统一由根目录的 settings.json 管理
# （可通过 Web 界面『配置』页修改），无需设置环境变量。
#
# 以下变量会被 app.py 读取，用于覆盖服务监听配置：
# ==========================================================

# 监听地址（默认 0.0.0.0 允许局域网访问）
# HOST=0.0.0.0

# 主监听端口（默认 5000）
# PORT=5000

# 额外监听端口（逗号分隔）。同一服务同时监听多个端口，
# 方便不同程序/设备各用一个入口调用。例如：
# EXTRA_PORTS=5001,5002
#
# 每个端口都提供完整 Web 界面 + 以下 HTTP 接口：
#   GET  /api/health            健康检查
#   POST /api/generate          流式生图（SSE，供前端）
#   POST /api/generate/sync     同步生图（JSON，供外部程序）
#   GET  /api/workflows         工作流列表
#   GET  /api/settings          读取配置
